package com.amelosbc.desafio_api_rest.domain.model;

import jakarta.persistence.Entity;

@Entity(name = "tb_feature")
public class Feature extends BaseItem {
    // Nenhum atributo adicional necessário no momento
}